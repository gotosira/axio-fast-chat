# 🔑 How to Get Your Gemini API Key

## Quick Steps:

1. **Visit Google AI Studio**
   👉 https://aistudio.google.com/app/apikey

2. **Sign in with your Google Account**

3. **Click "Create API Key"**

4. **Copy the API key**

5. **Create a `.env` file** in `/Users/sira/Documents/AXIO-FAST-CHAT/`

   ```bash
   GEMINI_API_KEY=your_copied_api_key_here
   PORT=3001
   ```

6. **Save the file**

7. **Run BaoBao!**
   ```bash
   npm start
   ```

## Important Notes:

- ✅ Gemini API is **FREE** for development use
- ✅ No credit card required for the free tier
- ✅ Very generous rate limits
- ⚠️  Keep your API key private (don't commit to git)

## After Setup:

Once you have your `.env` file with the API key, BaoBao will:
- ✨ Use **real Gemini AI** (no templates!)
- 🔍 Search your knowledge base automatically
- 💬 Generate smart, context-aware responses
- 🇹🇭 Respond in Thai with BaoBao's personality

---

The `.env` file is already in `.gitignore` so it won't be committed to version control.
